{application, mqtt_rest, [
	{description, "This is a RESTful API of MQTT server. The API manages user database of MQTT server."},
	{vsn, "1.0.0"},
	{registered, []},
	{applications, [
		kernel,
		stdlib,
		sasl,
		ssl,
		inets,
		jsx,
		jesse,
		lager,
		cowboy,
		ranch
	]},
	{included_applications, [
	]},
	{env, [
	]},
	{modules, []},
	{mod, {mqtt_rest, [{vsn, "1.0.0"}]}},
	{licenses, ["Apache 2.0"]},
	{links, []}
]}.
